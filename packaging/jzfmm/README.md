# jzfmm

GPU-native fast multipole method and differentiable N-body simulations in JAX. See the [documentation](https://jstuecker.github.io/jzfmm/) for installation instructions and examples.

Select the `cuda12` or `cuda13` extra to install the matching CUDA backend.
