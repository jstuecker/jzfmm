# Local Agent Instructions

- For Python-related commands in this repo, first activate:
  `source ~/.virtualenvs/uvjax/bin/activate`
- For Python package install/build operations, use `uv pip` rather than `python -m pip`.
- Always run unit tests from the `checks` directory.
- For full test sweeps, use `--quick`.
